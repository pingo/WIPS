CONTIKI = /opt/contiki
#CONTIKI = /home/erik/skola/wcnes/contiki

CONTIKI_SOURCEFILES += callbacks.c
all: meshtest meshtestsink netfloodtest netfloodtestsink phidgetstest

CFLAGS += -DPROJECT_CONF_H=1

include $(CONTIKI)/Makefile.include

